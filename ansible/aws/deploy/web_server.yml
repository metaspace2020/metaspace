---

- name: Deploying SM services code
  user: ubuntu
  hosts: web

  tasks:
    - name: Gather EC2 instance facts
      ec2_facts:
      tags: [always]

    - include: ../../deploy_tasks/sm_mol_db.yml
      tags: [sm_mol_db]

    - include: ../../deploy_tasks/sm_api.yml
      tags: [sm_api]

    - include: ../../deploy_tasks/sm_graphql.yml
      tags: [sm_graphql]

    - include: ../../deploy_tasks/sm_web_app.yml
      tags: [sm_web_app]

    - include: ../../deploy_tasks/sm_cluster_autostart.yml
      tags: [sm_cluster_autostart]
