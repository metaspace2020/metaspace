


#- name: Pull sources from the repository
#  git: repo=https://github.com/SpatialMetabolomics/sm-engine.git
#       dest={{ sm_home }}
#       version={{ sm_branch }}
#
#- name: Install SM into conda environment
#  args:
#    chdir: "{{ sm_home }}"
#    executable: /bin/bash
#  shell: "{{ activate_venv }} && pip install . -U"
#
#- include: restart_supervisor.yml
#
##    - name: Install all needed js libs
##      shell: npm install
##      args:
##        chdir: "{{ project_path }}/webapp"
#
##    - name: Compile static resources
##      shell: npm run build
##      args:
##        chdir: "{{ project_path }}/webapp"


